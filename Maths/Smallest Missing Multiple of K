class Solution {
public:
    int missingMultiple(vector<int>& nums, int k) {

        unordered_set<int> isPresent(nums.begin(), nums.end());
        for(int i=1;;i++){
            if(isPresent.find(k*i)==isPresent.end()) return k*i;
        }
        return -1; // dummy return 
    }
};

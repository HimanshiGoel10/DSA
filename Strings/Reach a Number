class Solution {
public:
    int reachNumber(int target) {
        if(target<0) target*=-1;
        int steps = 0;
        long long sum = 0;
        
        while(sum<target || ((sum-target)%2)){
            steps++;
            sum += steps;
        }

        return steps;
    }
};

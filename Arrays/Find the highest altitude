class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int sum = 0;
        int result = 0;

        for(auto &g: gain){
            sum+=g;
            result = max(result,sum);
        }

        return result;
    }
};
